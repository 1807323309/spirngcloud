#服务端口
server:
  port: 8761

# 是否注册自己到微服务，不需要注册自己
# 是否同步其他注册中心的数据
# 注册中心访问的地址
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/